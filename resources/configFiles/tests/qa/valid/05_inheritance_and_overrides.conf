server {
  listen 7000;
  root /srv/server-root;
  index index.server.html;
  client_max_body_size 5m;
  error_page 404 /errors/server-404.html;

  location / {
    autoindex on;
  }

  location /static {
    root /srv/static-root;
    index home.static.html;
    autoindex off;
  }
}
